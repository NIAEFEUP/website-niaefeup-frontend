services:
  website-niaefeup-frontend-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - 3000:3000
    volumes:
      - ./src:/app/src
      - ./static:/app/static
    environment:
      - PORT=3000
    network_mode: 'host'
